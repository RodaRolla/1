def is4etniy(x):
	ne4etnie = [1,3,5,7,9]
	for c in x:
		if c not in ne4etnie:
			break
		else: 
			return True
	return False
		
def parse(x)
	list = []
	for symbol in (x)
		print (symbol)
		if is4etniy(x)
			list.append(symbol)






k=int(input())
